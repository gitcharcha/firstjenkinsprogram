pipeline {
    agent any
    stages {
        stage('ipconfig') {
            steps {
                timeout (time:2 , unit : 'MINUTES' ) {
                    bat 'filelist.bat'
                }
            }
        }
        stage('nslookup') {
            steps {
               timeout (time:1 , unit : 'MINUTES' ) {
                bat 'Network.bat'
              }  
            }
        }
    }
}
